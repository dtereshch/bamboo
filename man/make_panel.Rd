% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_panel.R
\name{make_panel}
\alias{make_panel}
\title{Panel Data Structure Setting and Balancing}
\usage{
make_panel(data, index, delta = NULL, balance = NULL)
}
\arguments{
\item{data}{A data.frame containing panel data in a long format.}

\item{index}{A character vector of length 2 specifying the names of the entity and time variables.}

\item{delta}{An optional positive integer giving the expected interval between time periods.}

\item{balance}{One of \code{NULL} (default), \code{"entities"}, \code{"periods"}, or \code{"all"}. If not \code{NULL},
the panel is balanced according to the chosen method (see Details).}
}
\value{
The input data.frame with additional attributes, after possibly filtering or expanding rows.
Class \code{"panel_data"} is added.
}
\description{
This function adds panel structure attributes to a data.frame, storing entity and time variable names,
and optionally checks the expected interval between time periods. It can also balance the panel by
keeping only entities present in all periods, keeping only periods where all entities are present,
or creating all entity‑time combinations (filling missing combinations with \code{NA}).
}
\details{
This function adds attributes to a data.frame to mark it as panel data.
The returned object has class \code{"panel_data"} and includes the following attributes:
\describe{
\item{\code{metadata}}{List containing the function name and the arguments used
(\code{entity}, \code{time}, \code{delta}, and \code{balance} if provided).}
\item{\code{details}}{List with diagnostic vectors:
\describe{
\item{\code{entities}}{Unique values of the entity variable.}
\item{\code{periods}}{Sorted unique values of the time variable.}
\item{\code{excluded_rows}}{Rows removed due to missing entity/time (if any).}
\item{\code{entity_time_duplicates}}{Distinct duplicate combinations (if any).}
\item{\code{periods_restored}, \code{periods_missing}}{If \code{delta} is supplied and gaps are detected,
the full sequence and missing periods.}
}}
}

First, rows with missing values in the entity or time variables are removed and stored.
Then duplicate entity‑time combinations are checked; if \code{balance} is requested, duplicates are
automatically removed (first occurrence kept) and a message is issued.

When \code{delta} is specified, the time variable is coerced to numeric (if possible) and checked for
regular spacing. If gaps are detected, a message lists the missing periods and the full sequence
is stored.

\strong{Balancing the panel} (presence definition as in \code{describe_patterns}):
\describe{
\item{\code{balance = "entities"}}{Keep only entities present in \strong{all} time periods.}
\item{\code{balance = "periods"}}{Keep only time periods where \strong{all} entities are present.}
\item{\code{balance = "all"}}{Create a row for every entity‑time combination. If \code{delta} is supplied,
the full time sequence (including missing periods) is used. Missing combinations get \code{NA}
in all other columns.}
}
}
\examples{
data(production)
panel_data <- make_panel(production, index = c("firm", "year"))
panel_data2 <- make_panel(production, index = c("firm", "year"), delta = 1)
balanced_entities <- make_panel(production, index = c("firm", "year"), balance = "entities")
fully_balanced <- make_panel(production, index = c("firm", "year"), delta = 1, balance = "all")

}
\seealso{
\code{\link{describe_dimensions}}, \code{\link{describe_balance}}, \code{\link{describe_periods}}, \code{\link{describe_patterns}}
}
